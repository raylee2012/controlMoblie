# ML Kit 中文 OCR 替换 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 Google ML Kit 独立包替换 PaddleOCR ONNX Runtime，实现稳定离线中文 OCR，删除手写推理和模型下载逻辑。

**Architecture:** `ScreenOcr` 单例包装 `TextRecognizer`（ML Kit），`recognize()` 改为 `suspend` 函数用 `suspendCancellableCoroutine` 桥接 `Task<VisionText>`。三段式 `executeClick` 中 OCR 回调改为 `launch` 协程。

**Tech Stack:** ML Kit `text-recognition-chinese:16.0.1`，Kotlin Coroutines，Jetpack Compose

---

## File Structure

| 操作 | 文件 | 职责 |
|------|------|------|
| 修改 | `gradle/libs.versions.toml` | 版本声明 |
| 修改 | `app/build.gradle.kts` | 依赖声明 |
| 重写 | `app/src/main/java/com/controlmoblie/util/ScreenOcr.kt` | OCR 引擎（ML Kit 包装） |
| 重写 | `app/src/test/java/com/controlmoblie/util/ScreenOcrTest.kt` | 单元测试 |
| 删除 | `app/src/main/java/com/controlmoblie/util/OcrDict.kt` | 不再需要 |
| 修改 | `app/src/main/java/com/controlmoblie/service/ControlAccessibilityService.kt` | 调整 init 调用 + 协程 |
| 修改 | `app/src/main/java/com/controlmoblie/MainActivity.kt` | UI 适配新接口 |

---

### Task 1: 更新依赖声明

**Files:**
- Modify: `gradle/libs.versions.toml`
- Modify: `app/build.gradle.kts`

- [ ] **Step 1: 在 `libs.versions.toml` 添加 ML Kit 版本**

```toml
[versions]
# 在 versions 块中删除:
# tesseract = "9.1.0"

# 在 versions 块中新增:
mlkit-text-recognition = "16.0.1"

[libraries]
# 在 libraries 块中删除:
# tesseract-android = { group = "com.rmtheis", name = "tess-two", version.ref = "tesseract" }

# 在 libraries 块中新增:
mlkit-text-recognition-chinese = { group = "com.google.mlkit", name = "text-recognition-chinese", version.ref = "mlkit-text-recognition" }
```

- [ ] **Step 2: 在 `app/build.gradle.kts` 更新依赖**

```kotlin
dependencies {
    // ... 其他依赖保持不变 ...

    implementation(libs.vosk.android)
    implementation(files("libs/sherpa-onnx-1.13.2.aar"))
    implementation(libs.onnxruntime.android)
    implementation(libs.commons.compress)
    // 删除: implementation(libs.tesseract.android)
    implementation(libs.mlkit.text.recognition.chinese)  // 新增
    testImplementation("junit:junit:4.13.2")
    debugImplementation(libs.compose.ui.tooling)
}
```

- [ ] **Step 3: Commit**

```bash
git add gradle/libs.versions.toml app/build.gradle.kts
git commit -m "build: replace tesseract with mlkit-text-recognition-chinese"
```

---

### Task 2: 重写 ScreenOcrTest（RED）

**Files:**
- Modify: `app/src/test/java/com/controlmoblie/util/ScreenOcrTest.kt`

- [ ] **Step 1: 写入失败测试**

```kotlin
package com.controlmoblie.util

import org.junit.Assert.*
import org.junit.Test

class ScreenOcrTest {

    @Test
    fun `isReady returns false before init`() {
        assertFalse("should not be ready before init", ScreenOcr.isReady)
    }

    @Test
    fun `init sets isReady to true`() {
        ScreenOcr.release()
        ScreenOcr.init()
        assertTrue("isReady should be true after init", ScreenOcr.isReady)
    }

    @Test
    fun `release clears state`() {
        ScreenOcr.init()
        ScreenOcr.release()
        assertFalse("isReady should be false after release", ScreenOcr.isReady)
    }

    @Test
    fun `OcrResult stores coordinates correctly`() {
        val r = OcrResult("test", 10f, 20f, 100f, 50f)
        assertEquals("text", "test", r.text)
        assertEquals("x", 10f, r.x)
        assertEquals("y", 20f, r.y)
        assertEquals("width", 100f, r.width)
        assertEquals("height", 50f, r.height)
    }
}
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.\gradlew :app:testDebugUnitTest --tests com.controlmoblie.util.ScreenOcrTest
```
Expected: COMPILE ERROR — `init()` method no longer exists with new signature, or FAIL — `init()` sets isReady but implementation still has ONNX init logic.

- [ ] **Step 3: Commit**

```bash
git add app/src/test/java/com/controlmoblie/util/ScreenOcrTest.kt
git commit -m "test: rewrite ScreenOcrTest for ML Kit interface (RED)"
```

---

### Task 3: 重写 ScreenOcr.kt（GREEN）

**Files:**
- Modify: `app/src/main/java/com/controlmoblie/util/ScreenOcr.kt`

- [ ] **Step 1: 实现最小代码使测试通过**

```kotlin
package com.controlmoblie.util

import android.graphics.Bitmap
import android.util.Log
import com.google.mlkit.vision.common.InputImage
import com.google.mlkit.vision.text.TextRecognition
import com.google.mlkit.vision.text.chinese.ChineseTextRecognizerOptions
import kotlinx.coroutines.suspendCancellableCoroutine
import kotlin.coroutines.resume

data class OcrResult(val text: String, val x: Float, val y: Float, val width: Float, val height: Float)

object ScreenOcr {

    private const val TAG = "ScreenOcr"
    private var recognizer: com.google.mlkit.vision.text.TextRecognizer? = null
    var isReady: Boolean = false
        private set

    fun init() {
        if (isReady) return
        try {
            recognizer = TextRecognition.getClient(ChineseTextRecognizerOptions.Builder().build())
            isReady = true
            Log.d(TAG, "ML Kit TextRecognizer initialized")
        } catch (e: Exception) {
            Log.e(TAG, "ML Kit init failed", e)
            isReady = false
        }
    }

    suspend fun recognize(bitmap: Bitmap): List<OcrResult> = suspendCancellableCoroutine { cont ->
        val rec = recognizer
        if (rec == null) {
            cont.resume(emptyList())
            return@suspendCancellableCoroutine
        }
        val image = InputImage.fromBitmap(bitmap, 0)
        rec.process(image)
            .addOnSuccessListener { visionText ->
                val results = visionText.textBlocks.flatMap { block ->
                    block.lines.flatMap { line ->
                        line.elements.mapNotNull { elem ->
                            val box = elem.boundingBox ?: return@mapNotNull null
                            OcrResult(
                                text = elem.text,
                                x = box.centerX().toFloat(),
                                y = box.centerY().toFloat(),
                                width = box.width().toFloat(),
                                height = box.height().toFloat()
                            )
                        }
                    }
                }
                cont.resume(results.filter { it.text.isNotBlank() })
            }
            .addOnFailureListener { e ->
                Log.e(TAG, "OCR recognize failed", e)
                cont.resume(emptyList())
            }
    }

    fun release() {
        recognizer?.close()
        recognizer = null
        isReady = false
    }
}
```

- [ ] **Step 2: 运行测试，确认通过**

```bash
.\gradlew :app:testDebugUnitTest --tests com.controlmoblie.util.ScreenOcrTest
```
Expected: All 4 tests PASS.

- [ ] **Step 3: Commit**

```bash
git add app/src/main/java/com/controlmoblie/util/ScreenOcr.kt
git commit -m "feat: rewrite ScreenOcr with ML Kit TextRecognizer (GREEN)"
```

---

### Task 4: 更新 ControlAccessibilityService.kt

**Files:**
- Modify: `app/src/main/java/com/controlmoblie/service/ControlAccessibilityService.kt`

- [ ] **Step 1: 修改 onServiceConnected 中的 init 调用**

将第27-31行：
```kotlin
instance = this
val modelDir = File(filesDir, "paddle-ocr")
Log.d(TAG, "Accessibility service connected, modelDir exists=${modelDir.exists()}")
val ok = ScreenOcr.init(modelDir.absolutePath)
Log.d(TAG, "OCR init result=$ok, isReady=${ScreenOcr.isReady}")
```

改为：
```kotlin
instance = this
ScreenOcr.init()
Log.d(TAG, "Accessibility service connected, OCR isReady=${ScreenOcr.isReady}")
```

- [ ] **Step 2: 修改 executeClick 中的 OCR 回退（第217-258行）**

将：
```kotlin
// fallback 3: OCR screenshot
root.recycle()
if (!ScreenOcr.isReady) {
    ScreenOcr.init(File(filesDir, "paddle-ocr").absolutePath)
    Log.d(TAG, "OCR lazy init: isReady=${ScreenOcr.isReady}")
}
if (ScreenOcr.isReady && Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
    Log.d(TAG, "executeClick: trying OCR fallback for '${action.target}'")
    takeScreenshot(Display.DEFAULT_DISPLAY, mainExecutor, object : TakeScreenshotCallback {
        override fun onSuccess(screenshot: ScreenshotResult) {
            val bitmap = Bitmap.wrapHardwareBuffer(screenshot.hardwareBuffer, screenshot.colorSpace)
            if (bitmap == null) {
                screenshot.hardwareBuffer.close()
                onResult(false, "截屏失败")
                return
            }
            val results = ScreenOcr.recognize(bitmap)
            val match = results.find { it.text.contains(action.target) }
            if (match != null) {
                Log.d(TAG, "executeClick: OCR match '${action.target}' at (${match.x}, ${match.y})")
                performCoordinateClick(match.x.toInt(), match.y.toInt()) { clicked ->
                    onResult(clicked, if (clicked) "已点击 ${action.target}" else "无法点击 ${action.target}")
                }
            } else {
                Log.w(TAG, "executeClick: OCR found no match for '${action.target}'")
                onResult(false, "未找到 ${action.target}")
            }
            screenshot.hardwareBuffer.close()
        }
        override fun onFailure(errorCode: Int) {
            Log.e(TAG, "executeClick: screenshot failed, errorCode=$errorCode")
            onResult(false, "截屏失败")
        }
    })
    return
}
```

改为：
```kotlin
// fallback 3: OCR screenshot
root.recycle()
if (!ScreenOcr.isReady) {
    ScreenOcr.init()
    Log.d(TAG, "OCR lazy init: isReady=${ScreenOcr.isReady}")
}
if (ScreenOcr.isReady && Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
    Log.d(TAG, "executeClick: trying OCR fallback for '${action.target}'")
    takeScreenshot(Display.DEFAULT_DISPLAY, mainExecutor, object : TakeScreenshotCallback {
        override fun onSuccess(screenshot: ScreenshotResult) {
            val bitmap = Bitmap.wrapHardwareBuffer(screenshot.hardwareBuffer, screenshot.colorSpace)
            if (bitmap == null) {
                screenshot.hardwareBuffer.close()
                onResult(false, "截屏失败")
                return
            }
            kotlinx.coroutines.CoroutineScope(kotlinx.coroutines.Dispatchers.Main).launch {
                val results = ScreenOcr.recognize(bitmap)
                val match = results.find { it.text.contains(action.target) }
                if (match != null) {
                    Log.d(TAG, "executeClick: OCR match '${action.target}' at (${match.x}, ${match.y})")
                    performCoordinateClick(match.x.toInt(), match.y.toInt()) { clicked ->
                        onResult(clicked, if (clicked) "已点击 ${action.target}" else "无法点击 ${action.target}")
                    }
                } else {
                    Log.w(TAG, "executeClick: OCR found no match for '${action.target}'")
                    onResult(false, "未找到 ${action.target}")
                }
                screenshot.hardwareBuffer.close()
            }
        }
        override fun onFailure(errorCode: Int) {
            Log.e(TAG, "executeClick: screenshot failed, errorCode=$errorCode")
            onResult(false, "截屏失败")
        }
    })
    return
}
```

同时更新顶部的 import：
```kotlin
// 删除: import java.io.File
// 新增:
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.launch
```

并在 OCR 回调中将完全限定名替换为简短形式：
```kotlin
// 将
kotlinx.coroutines.CoroutineScope(kotlinx.coroutines.Dispatchers.Main).launch {
// 改为
CoroutineScope(Dispatchers.Main).launch {
```

- [ ] **Step 3: 编译检查，确认无语法错误**

```bash
.\gradlew :app:compileDebugKotlin
```
Expected: BUILD SUCCESSFUL（或仅有未使用 import 警告，再清理即可）

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/controlmoblie/service/ControlAccessibilityService.kt
git commit -m "refactor: update ControlAccessibilityService for ML Kit OCR"
```

---

### Task 5: 更新 MainActivity.kt

**Files:**
- Modify: `app/src/main/java/com/controlmoblie/MainActivity.kt`

- [ ] **Step 1: 修改 OCR 状态变量（第63行）**

```kotlin
// 删除:
var ocrModelReady by remember { mutableStateOf(ScreenOcr.isModelReady(this@MainActivity)) }
var ocrDownloadProgress by remember { mutableStateOf(-1f) }
var ocrDownloading by remember { mutableStateOf(false) }

// 替换为:
var ocrReady by remember { mutableStateOf(ScreenOcr.isReady) }
```

- [ ] **Step 2: 修改 OCR UI 区域（第182-218行）**

```kotlin
// 删除整个下载进度+下载按钮+状态区域（第182-218行），替换为:
Button(
    onClick = {
        ScreenOcr.init()
        ocrReady = ScreenOcr.isReady
    },
    modifier = Modifier.fillMaxWidth()
) {
    Text("初始化 OCR 识别")
}

if (ocrReady) {
    Text("OCR识别 ✓", color = MaterialTheme.colorScheme.primary,
        style = MaterialTheme.typography.bodySmall)
}
```

同时删除顶部不再需要的 import：
```kotlin
// 删除: import kotlinx.coroutines.Dispatchers
```

- [ ] **Step 3: 编译检查**

```bash
.\gradlew :app:compileDebugKotlin
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/controlmoblie/MainActivity.kt
git commit -m "refactor: update MainActivity OCR UI for ML Kit"
```

---

### Task 6: 删除 OcrDict.kt

**Files:**
- Delete: `app/src/main/java/com/controlmoblie/util/OcrDict.kt`

- [ ] **Step 1: 删除文件**

```bash
git rm app/src/main/java/com/controlmoblie/util/OcrDict.kt
```

- [ ] **Step 2: Commit**

```bash
git commit -m "refactor: remove OcrDict (no longer needed with ML Kit)"
```

---

### Task 7: 构建验证

- [ ] **Step 1: 完整编译 Debug APK**

```bash
.\gradlew :app:assembleDebug
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 2: 运行单元测试**

```bash
.\gradlew :app:testDebugUnitTest
```
Expected: All tests PASS，特别是 ScreenOcrTest 4/4。

- [ ] **Step 3: Commit（如有修改）**

```bash
git add -A
git commit -m "chore: final build verification after ML Kit OCR migration"
```
